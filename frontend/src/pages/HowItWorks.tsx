import { ArrowRight, Check, Eye, FileInput, ScanSearch } from "lucide-react";
import type { LucideIcon } from "lucide-react";
import { Link } from "react-router-dom";

const steps: { icon: LucideIcon; number: string; title: string; copy: string }[] = [{ icon: FileInput, number: "01", title: "Submit", copy: "Paste an email or message, enter a URL, or select an attachment for review." }, { icon: ScanSearch, number: "02", title: "Analyze", copy: "Multiple signals are evaluated, including language, sender context, links, and file type." }, { icon: Eye, number: "03", title: "Understand", copy: "Get a risk level, supporting indicators, and a direct recommended action." }];

export default function HowItWorks() {
  return <main data-testid="how-it-works-page"><section className="border-b border-[#dce5ea] bg-white"><div className="mx-auto max-w-[1240px] px-5 py-16 lg:px-8 lg:py-24"><p className="text-[11px] font-extrabold uppercase tracking-[0.18em] text-[#087f82]">How it works</p><h1 className="mt-3 max-w-[700px] text-4xl font-extrabold tracking-[-0.05em] sm:text-5xl" data-testid="how-it-works-heading">A clearer path from suspicion to action.</h1><p className="mt-5 max-w-[650px] text-base leading-7 text-[#52636d]" data-testid="how-it-works-intro">NoSpamHQ turns uncertain content into a structured assessment you can understand and act on.</p></div></section><section className="mx-auto max-w-[1240px] px-5 py-16 lg:px-8 lg:py-24"><div className="grid gap-8 md:grid-cols-3">{steps.map(({ icon: Icon, number, title, copy }) => <div key={number} className="border-t-2 border-[#0fb7b5] pt-6" data-testid={`how-it-works-step-${number}`}><Icon className="size-6 text-[#087f82]" /><p className="mt-8 font-mono text-xs font-bold text-[#8fa2ad]">{number}</p><h2 className="mt-2 text-xl font-extrabold" data-testid={`how-it-works-step-${number}-title`}>{title}</h2><p className="mt-3 text-sm leading-6 text-[#52636d]">{copy}</p></div>)}</div></section><section className="border-y border-[#dce5ea] bg-[#eef3f6]"><div className="mx-auto grid max-w-[1240px] gap-12 px-5 py-16 lg:grid-cols-[.8fr_1.2fr] lg:px-8 lg:py-20"><div><p className="text-[11px] font-extrabold uppercase tracking-[0.18em] text-[#087f82]">Designed for clarity</p><h2 className="mt-3 text-3xl font-extrabold tracking-[-0.04em]" data-testid="how-it-works-clarity-heading">The report is the product.</h2></div><div className="grid gap-5 sm:grid-cols-2">{["What was detected", "Why it matters", "Which indicators contributed", "What to do next"].map((item) => <div key={item} className="flex gap-3 border-b border-[#cbd9df] pb-4"><Check className="size-4 shrink-0 text-[#087f82]" /><p className="text-sm font-bold text-[#26343d]" data-testid={`how-it-works-benefit-${item.toLowerCase().replaceAll(" ", "-")}`}>{item}</p></div>)}</div></div></section><section className="mx-auto flex max-w-[1240px] flex-col items-start justify-between gap-5 px-5 py-14 sm:flex-row sm:items-center lg:px-8"><div><h2 className="text-2xl font-extrabold tracking-[-0.04em]">Ready to inspect something?</h2><p className="mt-2 text-sm text-[#52636d]">Try the workflow without creating an account.</p></div><Link to="/demo" className="inline-flex items-center gap-2 rounded-[7px] bg-[#087ea4] px-5 py-3 text-sm font-extrabold text-white" data-testid="how-it-works-demo-button">Open demo <ArrowRight className="size-4" /></Link></section></main>;
}